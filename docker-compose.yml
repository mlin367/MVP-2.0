version: '3'
services: 
  db: 
    restart: always
    image: mongo
    ports: 
      - '27017:27017'
    volumes:
      - ./docker/data/db:/var/lib/mongo
  app:
    restart: always 
    build: .
    ports:
      - '8008:1337'
    depends_on:
      - db 